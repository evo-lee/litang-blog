export default function Loading() {
  return (
    <div className="loading-panel">
      <p className="meta-note">Loading</p>
      <h1>Preparing the page...</h1>
      <p>The content graph is resolving and the route shell is being assembled.</p>
    </div>
  );
}
