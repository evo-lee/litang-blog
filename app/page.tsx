// Phase 0 placeholder — will be replaced in Phase 2 with the real homepage.
// A minimal page is required so that `next build` has at least one route to compile.

export default function HomePage() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>evolee</h1>
      <p>Under construction — Phase 0 scaffold complete.</p>
    </main>
  );
}
