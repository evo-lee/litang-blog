---
title: About
description: A sample standalone page for the content loader.
---

# About

这是一个示例独立页面，用来验证 `content/pages` 的读取与渲染流程。

## Scope

The page loader shares the same Markdown pipeline as posts, but it uses a smaller frontmatter schema.

